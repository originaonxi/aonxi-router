# ── ANTHROPIC (outreach, content, objection handling — NEVER downgrade) ──
# console.anthropic.com → API Keys
ANTHROPIC_API_KEY=

# ── GOOGLE GEMINI (PKM + reply classification — 85% vs Haiku 70%) ──
# aistudio.google.com → Get API Key  (FREE tier works)
GEMINI_API_KEY=

# ── XAI GROK (X/Twitter real-time signals + scoring + summaries) ──
# console.x.ai → API Keys
# UNIQUE: only model with live X data. AROS signals 24-48h ahead of competitors.
GROK_API_KEY=

# ── MOONSHOT KIMI K2.5 (ARIA capital research + competitor web crawling) ──
# platform.moonshot.cn → API Management
# 300+ stable tool calls. Transforms ARIA from emailer to intelligence system.
KIMI_API_KEY=

# ── OPENAI GPT-4o (optional: complex negotiation >10 steps) ──
# platform.openai.com → API Keys
OPENAI_API_KEY=

# ── AIRTABLE (router decision log → v2 training data) ──
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
